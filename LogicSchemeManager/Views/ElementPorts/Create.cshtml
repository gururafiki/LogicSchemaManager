@model LogicSchemeManager.Models.ElementPort

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>ElementPort</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="ElementId" class="control-label"></label>
                <select asp-for="ElementId" class ="form-control" asp-items="ViewBag.ElementId"></select>
            </div>
            <div class="form-group">
                <label asp-for="ParentId" class="control-label"></label>
                <select asp-for="ParentId" class ="form-control" asp-items="ViewBag.ParentId">
                    <option value="">Select one if Port is input and it connected to another element</option>
                </select>
            </div>
            <div class="form-group">
                <label asp-for="PortId" class="control-label"></label>
                <select asp-for="PortId" class ="form-control" asp-items="ViewBag.PortId"></select>
            </div>
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
